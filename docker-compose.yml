version: '3.8'

services:
  quantect-platform:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quantect-cbom
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
